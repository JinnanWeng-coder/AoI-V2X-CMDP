# claim4_support — 300-ep limit-cycle / headline data (to be superseded at ep600)

The **300-episode** `t8e10` integral-vs-PID runs that back **Claim 4** (PID-Lagrangian
damps the integral dual's limit-cycle) and the original n=10 headline / CI. Archived here
because a **600-ep support for Claim 4 is planned** to replace this 300-ep version.
Current findings + live data→claim map: `../../../CLAUDE.md`.

## What's here
- **Runs (30):** the full 300-ep headline set, seeds 2–11 — `marl_model_soft_seed{2-11}_base`
  (soft baseline), `marl_model_hard_seed{2-11}_t8e10` (integral) + `_t8e10_pid` (PID).
  Claim-4 metric: worst platoon's per-episode `viol_rate.mat` last-100-ep std ≈ 0.175
  (integral) → 0.097 (PID), concentrated on seeds 3,7. soft-vs-hard = the n=10 CI headline.
  (The LIVE baseline is the separate `soft_*_base_ep600` in `model/` root — NOT here.)
- **Report:** `RQ1_REMOTE_REPORT.md` (the integral-era headline + τ/ε phase + floor write-up;
  several numbers later superseded by the ep600 canon).
- **Figures:** `fig_headline_violation.png`, `fig_lambda.png`, `fig_cost_tradeoff.png`
  (RETIRED integral/300-ep cost), `fig_softsweep.png` (soft τ-calibration).
- **`scripts/`:** `campaign_driver.ps1`, `make_figures.py`, `finalize_report.py` (the
  headline-batch tooling, archival).

## Notes
- The canonical claim-4 manuscript figure is `../../../../Manuscript/figures/fig_claim4_pid_stability.png`
  (generated by `Manuscript/make_claim_figures.py`, which still reads these runs — its path
  must be repointed here, or it will be replaced by the planned ep600 support).
- Shared loader `analyze_remote.py` and generic utilities (`check_status.ps1`,
  `wait_wave.ps1`, `launch_runs.ps1`) stay in `../../../results_remote/scripts/`
  (used by live batches). Re-running the archived scripts here would need their paths fixed.
- The other 300-ep retired studies (τ/ε phase non-(8,10), σ-anneal, floor) are one level up
  in `../` (Legacy_300ep/ root).
